{{ include ../parts/header-html }}

<div>
{{= gm.player }} a validé une partie de {{= setting }} le {{= date }} {{= timeframe }} avec:
<ul>
 {{ players.forEach( function (player) { }}
	<li> {{= player.player }} 
	{{ if (player.idcomment) { }}
<span class="comment">({{= player.comment }})</span>

{{ } }}

	</li>
 {{ }) }}
</ul>
</div>

<div>
Nom de la partie: {{= game.title }}
</div>

<div>
Heure de début: {{= game.time }}
</div>

{{ include ../parts/footer-html }}

